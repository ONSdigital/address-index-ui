import { getPageLocalValues } from "/static/js/f_helpers/local_storage_page_helpers.mjs";
import { crEl } from "/static/js/f_helpers/element_creation.mjs";

export function setupClearButton(page_name) {
  // Get a handle on the clear button

  // Prevent default action of visiting a link

  // Add the action to reset the 'page previously searched values' in local storage 

  // Hide or delete/refresh the results message and table

  console.log('Setting up clear button for page:', page_name);
}