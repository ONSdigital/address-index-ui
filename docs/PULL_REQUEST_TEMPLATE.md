## Version Bump Checklist

# If any files not in .github were changed:
- [ ] I have updated `version.txt` with a new version number.
- [ ] I have verified that the new version follows semantic versioning (`MAJOR.MINOR.PATCH`).

# If only files in .github were changed:
- [ ] I have verified that no changes were made to files outside of the .github directory

## Additional Notes
Test images can be created with the manual GitHub Action Workflow `Create a UI test image for AP`. 
Test images must only be deployed to AP.